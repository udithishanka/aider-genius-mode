# A simple Jac program to greet a user
def greet(name):
    return "Hello, " + name + "!"

# Main function
def main():
    user_name = "World"
    print(greet(user_name))

# Execute the main function
main()
